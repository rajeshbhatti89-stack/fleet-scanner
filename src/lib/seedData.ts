import { Vehicle, Operator, MeterLog } from '../types';

// Production clean state — No mock data
export const INITIAL_VEHICLES: Vehicle[] = [];
export const INITIAL_OPERATORS: Operator[] = [];
export const INITIAL_LOGS: MeterLog[] = [];
